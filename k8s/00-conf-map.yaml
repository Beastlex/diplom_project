apiVersion: v1
kind: ConfigMap
metadata:
  name: proj-config
data:
  engine: "POSTGRESQL"
  db: "statdata"
  user: "postgres@10.0.2.4"
  port: "5432"
  host: "10.0.2.4"
  args: "--auth-host=scram-sha-256"
